scar:
  id: SCAR-2026-0412-FRONTMATTER-DRIFT
  type: schema-sync-gap
  triggered_by: "Meta_Kim capability verification run 2026-04-12"
  what_happened: >
    Canonical agents missing YAML frontmatter fields (do/own/boundary/trigger)
    — boundary info exists in body text only, may not survive cross-runtime sync.
  root_cause: >
    Agent body text had Own/Do Not Touch sections, but YAML frontmatter
    was not updated to include structured boundary fields, making
    programmatic frontmatter extraction impossible.
  impact: degraded
  prevention_rule: >
    All new canonical agents must include own, do_not_touch, boundary,
    and trigger fields in YAML frontmatter before being committed.
  detected_by: meta-prism agent quality audit
  severity: medium
  status: closed
  resolution: Added own/do_not_touch/boundary/trigger to all 8 canonical agents (2026-04-12)
